@model IEnumerable<MVC2022.Models.Lanche>

    <div>
        <h1>
            @ViewData["Titulo"]
        </h1>
    </div>
    <h2> 
        @ViewData["Time"]
    </h2>
    <hr />
    <h3>@ViewBag.total @ViewBag.TotalCount</h3>
    <hr />
    <h3>@TempData["Nome"]</h3>

    @foreach(var lanche in Model)
{
    <div>
        <h4>
            @lanche.Nome
        </h4>
        <p><img src="@lanche.ImagemUrl"> </p>
        <h3>Preco : @lanche.Preco.ToString("c")</h3>
        
    </div>
}